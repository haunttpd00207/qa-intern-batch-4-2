<div class="row questions">
  <div class="col-md-8 questions">
    <blockquote class="blockquote">
      <p class="question">
        <%= link_to question.title, question %>
      </p>
      <p class="anwser">
        <%= raw question.content %>
      </p>
      <div class="tags">
        <%= (render partial: "shared/tags_list", collection: question.tags,
        as: :tag) unless question.tags.blank? %>
      </div>
    </blockquote>
    <div class="bottom-questions">
      <div class="picture-user-question">
        <%= image_tag question.user.picture.url %>
      </div>
      <%= link_to question.user.name, question.user %>
      <span class="time"> | Asked on: <%= question.created_at %> </span>
    </div>
  </div>
  <div class="col-md-4 col-xs-4 q-right-content">
    <ul class="question-statistic">
      <li class="active">
        <span itemprop="answerCount" class="question-answers">
          <%= question.answers.count %>
        </span>answers
      </li>
      <li>
        <span itemprop="upvoteCount" class="question-votes">
          <%= pluralize(question.votes.count, "") %>
        </span> votes
      </li>
    </ul>
  </div>
  <div>
    <% if current_user?(question.user) %>
      <span class="badge badge-primary-questions"><%= link_to "Edit", edit_question_path(question) %></span>
      <span class="badge badge-primary-questions"><%= link_to "Delete", question_path(question), method: :delete %></span>
    <% end %>
  </div>
</div>
